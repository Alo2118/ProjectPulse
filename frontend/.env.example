# Backend API URL
# Default: backend esposto via nginx sul dominio interno
# Se servi frontend+api sullo stesso host, lascia /api.
# Per sviluppo locale usa .env.development (già su http://localhost:3001/api).
VITE_API_URL=http://projectpulse.local/api
